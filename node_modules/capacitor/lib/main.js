(function() {
  define(['dispatcher', 'action-manager', 'store', 'invariant'], function(dispatcher, actionManager, Store, invariant, logger) {
    return {
      dispatcher: dispatcher,
      actionManager: actionManager,
      Store: Store,
      invariant: invariant
    };
  });

}).call(this);
